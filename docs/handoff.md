# 会话接手说明（Handoff）

> **本文件只记「当前会话手头正在干的活」**，供会话意外丢失后接得上。
>
> - 整体进度、完成定义、验证基线、各条目的实现与复审记录 → [`core-capability-roadmap.md`](core-capability-roadmap.md)、[`industry-capability-roadmap.md`](industry-capability-roadmap.md)
> - 流程铁律（TDD、子代理、双复审、合并、部署、凭据边界）→ 项目根 `CLAUDE.md`
>
> 上面两处有的，这里不重复。**任务做完并合入后，把它从本文件删掉**——这里不留历史。

---

## 快照时间：2026-07-17

**main** = `a4b2b03`（已推送）｜**迁移单头** `20260716_0037`，下一个可用号 `20260716_0038`
**常驻栈** `agent-platform-dev` 13 服务健康，Demo Seed 已重放｜账号 `demo@example.com` / `agent-platform-demo`｜API 18000、前端 18080、PG 15432、MinIO 19000

## 🔴 在途：T9 二轮双复审 + T3 整改轮，共 3 个代理

**接手第一步——先确认它们死活，别凭工作树有改动就假定还在跑**（CLAUDE.md「长任务防假忙与中断恢复」）：

```bash
git worktree list && for w in wt/*/; do echo "--- $w"; git -C "$w" status --short; done
ps aux | grep -E "pytest|playwright" | grep -v grep
docker ps -a --format "{{.Names}}\t{{.Status}}"
```

各代理**零文件重叠**，都被要求**不 commit/不 push**（所以工作区有改动是正常的）。若确认代理已死且有改动，**先读完整差异再决定接管或重派**，别直接覆盖。

⚠️ 看到随机名容器（如 `*-mvp-test-<pid>-*`、`t8-pg*`）**先查 `Status` 再动手**——曾差点把某代理刚起 3 秒的验收栈当残留清掉。

### [T9] `task/t9-pg-fixture-isolation` @ `421c88b` —— PG 夹具隔离｜**一轮 FAIL 已整改，二轮双复审在跑**

一轮：质量 FAIL（2 Important）、规格 PASS（4 LOW）。已按根因集中修：①门禁补孤岛表；②TRUNCATE 加 `lock_timeout=5s`；③措辞收敛；④护栏拒清含 Demo Seed 的库。

**这轮最值得记的**：`run_dead_letters` / `tool_audit_events` 的 tenant_id/user_id 是**裸 uuid 列、无 FK 约束**，CASCADE 永远够不到——而门禁只种了 CASCADE 可达的表，导致「退回手工清单」变异**照样全绿**。**门禁对它自己要根治的那类缺陷全瞎。** 实现一直是对的（这两张表在 metadata 里、被直接列进 TRUNCATE），瞎的是门禁。

**护栏方案是子代理证伪我之后定的**：我建议库名/端口白名单——实测行不通，一次性验收栈与常驻 `agent-platform-dev` **同名 `agent_platform`、同在 127.0.0.1、端口随机**，两个判据都没区分力。改用语义判据：库里有 Demo Seed 账号即拒绝。

**顺带挖出的潜伏问题**：`test-mvp-profile.sh` 在同一个库上 :217 跑 demo_seed、:440 跑 PG 集成测试。那 3 个并发文件按全库计数断言、要求整库清理，**与已 seed 的栈根本不兼容**，只是今天恰好只跑了 2 个不调 `reset_database` 的用例才没炸（整改前就成立）。护栏把静默毁数据变成响亮报错。

**跑完我要做**：两个都 PASS → 合并（同一提交带 roadmap 第 6 节改写）→ 删 `wt/t9-review`。

### [T3] `task/c12-frontend` @ `1b5aec4` —— C12 阶段二｜**双复审均 FAIL，整改轮在跑**

**质量复审 FAIL——`zoned-time.ts` 两个高危正确性缺陷**（同根：二趟法假设不动点存在且唯一，缺口内无解、重叠内两解，既不检测也不消歧）：
1. **春季不存在的当地时间静默返回错误时刻，且与代码自己的注释方向相反**：填美东 `03-08T02:30` → 得 `06:30Z`（=01:30 EST，切换**前**），注释却写「落到切换后」。用户填 02:30 拿到 01:30，**早 1 小时、无报错**。
2. **往返漂移 1h，命中真实用户路径**：fall-back 重复小时的第二次出现，UTC→当地→UTC 漂 -1h。编辑表单正是这条路径——**用户编辑一条该时段的 `once` 任务、不碰时间字段直接保存 → 任务被静默提前 1 小时**。
   **现有往返测试是教科书式假绿**：样本选了同一 fold 的**第一次**出现，与失败样本仅差 1 小时。146 个测试全绿的同时函数在静默返错。

**规格复审 FAIL——完成定义第 5 条其实没满足**：原文点名「创建、**编辑**、**暂停**和执行记录」，但编辑/暂停**只有组件测试**，Playwright 里一次都没点过。且台账「剩余未完成」只写了配额、**把这个缺口漏了**（触碰「禁止用文档措辞掩盖代码缺口」）。

**已要求整改时利用两者交集**：编辑 E2E 专门用一条落在 DST fall-back 重复小时的 `once` 任务——早有这条用例，缺陷 2 根本跑不掉。

**另**：竞态用例有假绿面（「无重复 scheduled_for」在只有一个副本真 tick 时同样成立，没有断言证明两副本争抢过同一触发点）。

**我推翻了自己两处**：
- `video-studio-media-library.spec.ts` 进 testIgnore —— 我先批了，质量复审指出属夹带，**它对**（我的理由是便利不是必要）。**合并时我把这行拆成独立提交**，T3 不用动。
- 「无 `runs.execute` 看不到入口」—— 我核了台账原文，第 6 条只写「时区、重启恢复、重复触发和权限 E2E 通过」，**没点名那个子句**，是我在本文件转述时自己加的。规格复审判「无对象」成立，T3 的处理正确。

**跑完我要做**：基于新 HEAD 重派双复审（整改后两个都要重跑全分支）。**C12 标 ✅ 仍差 C16 阶段三配额接入 + 本轮编辑/暂停 E2E，两者必须同时闭合。**

## 接下来的排队

> **用户已授权自主连续推进 C 系列**（2026-07-17）：按 roadmap 依赖图往下走，不用每条来问；能并行的并行，有直接依赖的串行。**B 系列暂不启动。**
>
> 自主不等于降标准——完成定义、双复审、真实边界验收、台账同步一条都不减。真受阻或需要产品取舍时才找用户。

1. **[T4] C16 阶段二**（用量/成本，纯观测面）—— T8 已合入，阻塞解除。**等 T9 或 T3 腾出槽位再开**：它会动 `tests/integration/model_gateway/`，与 T9 正在改的夹具重叠。
2. **[T5] C16 阶段三**（预算/配额/限流/告警 + fallback + **C12 配额门禁接入**）—— C12 标 ✅ 卡在这里。

再往后的顺序见 roadmap。**Core 并行槽位上限 2 个 🚧 条目**（C12、C16 已占满）；红线修复/复审这类不占槽位，但**实测同时跑 2 个写入代理是舒适上限，3 个开始漏审**。
